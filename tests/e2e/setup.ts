import { test as setup } from '@playwright/test'

/**
 * E2Eテストのグローバルセットアップ
 *
 * すべてのテスト実行前に一度だけ実行される
 */

setup('グローバルセットアップ', async () => {
  // 将来的に必要な場合、ここにグローバルセットアップ処理を追加
  // 例: 認証トークンの取得、テストデータベースの初期化など
})
